#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two string
 * @s1: first string
 * @s2: second string
 * Return: an integer
 */
int _strcmp(char *s1, char *s2)
{
	/* strcmp() functions to compare */
	return (strcmp(s1, s2));
}
