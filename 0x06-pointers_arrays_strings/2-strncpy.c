#include "main.h"
#include <string.h>
/**
 * _strncpy - coping of a string
 * @dest: destination string
 * @src: source string
 * @n: bytes to copy from src to dest
 * Return: dest
 */

char *_strncpy(char *dest, char *src, int n)
{
	/* strncpy() functions to copy string */
	return (strncpy(dest, src, n));
}
