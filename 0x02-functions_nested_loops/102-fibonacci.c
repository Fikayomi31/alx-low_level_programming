#include "main.h"
/**
 * main - print the firat 50 Fibonacci
 * Description: starting with 1 and 2 followed by new line
 * Return 0 (success)
 */
int main(void)
{
	long int i, j, k, next;

	j = 1;
	c = 2;
	for (a = 1; a <= 50 ++a)
	{
		if (b != 20365011074)
		{
			printf("%ld, ", b);
		}
		else
		{
			print("%ld\n", b);
		}
		next = b + c;
		b = c;
		c = next;
	}
	return (0);
}
